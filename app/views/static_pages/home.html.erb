<% if logged_in? %>
  <div class="row">
      <aside class="col-md-4">
          <section class="user_info">
              <%= render 'shared/user_info' %>
          </section>
          <section class="neomictopost_form">
              <%= render "shared/neomicropost_form" %>
          </section>
      </aside>
  </div>
 <% else %>
   <div class="center jumbotron">
       <h1>Welcome to the NeoMicroposts!!</h1>
       
       <%= link_to "Sign up Now!!", signup_path, class: "btn btn-lg btn-primary" %>
   </div>
<% end %>